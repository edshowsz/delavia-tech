services:
  delavia-tech:
    build: .
    image: delavia-tech:local
    container_name: delavia-tech
    restart: unless-stopped
    ports:
      - "127.0.0.1:3080:80"
    networks:
      - proxy

networks:
  proxy:
    external: true
    name: delavia-proxy
